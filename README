# Crypto Price Prediction Model
## Getting Started
1. Clone the repository to your local machine:
    ```bash
    git clone https://github.com/vivekbopaliya/crypto-price-predictor
    ```
2. Create and activate a virtual environment:
    ```bash
    python -m venv venv
    source venv/bin/activate  # On Windows: venv\Scripts\activate
    ```
3. Install dependencies:
    ```bash
    pip install -r requirements.txt
    ```
4. Create a `.env` file in the root directory of the project and paste the following environment variables:
    ```plaintext
   CLIENT_KEY
   CLIENT_SECRET
    ```
5. For this you will have to make an account in Binance and get the API credentilals. 

6. Start the prediction server:
    ```bash
    python main.py
    ```

And you are good to go!

